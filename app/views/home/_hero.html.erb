<section class="py-20 text-center bg-gradient-to-br from-orange-50 via-amber-50 to-yellow-50">
  <div class="max-w-3xl mx-auto px-4">
    <div class="space-y-6">
    <h1 class="text-3xl sm:text-5xl lg:text-7xl font-bold leading-tight text-orange-700">
    "前にもこれ食べたな"
    </h1>
    <!--<p class="text-xl sm:text-2xl text-gray-600 mt-4">
      注文時の悩みを記録で解決
    </p>-->
    <p class="text-xl sm:text-2xl lg:text-3xl text-gray-600 font-medium max-w-3xl mx-auto">
      注文時の悩みを<span class="text-orange-700 font-bold">記録</span>で解決
    </p>

    <p class="text-base sm:text-lg text-gray-600 mt-4 max-w-2xl mx-auto leading-relaxed">
        「次回頼もうと思っていたメニューはどれだっけ？」<br class="hidden sm:block">
        そんな経験を記録と振り返りで解決します
    </p>
    </div>


    <div class="mt-12 flex flex-col sm:flex-row gap-4 justify-center items-center">
      <% if user_signed_in? %>
        <%= link_to "記録をはじめる", orders_path, 
            class: "bg-amber-500 text-white text-lg sm:text-xl font-bold py-4 px-8 rounded-full hover:bg-orange-400 transition" %> 
      <% else %>   
        <%= link_to "記録をはじめる", new_user_registration_path, class: "bg-amber-500 text-white text-lg sm:text-xl font-bold py-4 px-8 rounded-full hover:bg-orange-400 transition" %>
      <% end %>
    </div>
  </div>
</section>